{
  "name": "learning-path-planner",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Planner local-first e personalizzabile per percorsi di apprendimento.",
  "scripts": {
    "build": "node scripts/build-classic.mjs",
    "test": "node scripts/build-classic.mjs --check && node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
